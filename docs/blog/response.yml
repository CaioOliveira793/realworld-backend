profile_list:
  description: Profile list
  content:
    application/json:
      schema:
        allOf:
        - $ref: '../component/schema.yml#/OffsetPagination'
        - properties:
            data:
              items:
                $ref: 'resource.yml#/Profile'
profile_resource:
  description: Profile resource
  content:
    application/json:
      schema:
        $ref: 'resource.yml#/Profile'

follow_resource:
  description: Follow resource
  content:
    application/json:
      schema:
        $ref: 'resource.yml#/Follow'
follow_list:
  description: Follow list
  content:
    application/json:
      schema:
        allOf:
        - $ref: '../component/schema.yml#/OffsetPagination'
        - properties:
            data:
              items:
                $ref: 'resource.yml#/Follow'

article_resource:
  description: Article resource
  content:
    application/json:
      schema:
        $ref: 'resource.yml#/Article'
article_list:
  description: Article list
  content:
    application/json:
      schema:
        allOf:
        - $ref: '../component/schema.yml#/OffsetPagination'
        - properties:
            data:
              items:
                $ref: 'resource.yml#/Article'

article_favorite_resource:
  description: Article favorite resource
  content:
    application/json:
      schema:
        $ref: 'resource.yml#/ArticleFavorite'
article_favorite_list:
  description: Article favorite list
  content:
    application/json:
      schema:
        allOf:
        - $ref: '../component/schema.yml#/OffsetPagination'
        - properties:
            data:
              items:
                $ref: 'resource.yml#/ArticleFavorite'
